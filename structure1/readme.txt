fully observable condition
measurements are power injections at each bus

input x: load demands at each bus
output y: generation at each bus; 0 means no generation

It seems that many 0s in y makes the gradient descent problems. 
